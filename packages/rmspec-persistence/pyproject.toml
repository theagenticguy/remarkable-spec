[project]
name = "rmspec-persistence"
version = "0.2.0"
description = "SQLite repository adapters. The only package permitted to import sqlite3"
readme = "README.md"
authors = [
    { name = "Laith Al-Saadoon", email = "9553966+theagenticguy@users.noreply.github.com" }
]
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "rmspec-domain",
]

[build-system]
requires = ["uv_build>=0.11.12,<0.12.0"]
build-backend = "uv_build"

# PEP 420 namespace: distribution rmspec-persistence ships the rmspec.persistence subpackage,
# so all nine members coexist under one `rmspec` namespace with no
# rmspec/__init__.py to fight over.
[tool.uv.build-backend]
module-name = "rmspec.persistence"
namespace = true

[tool.uv.sources]
rmspec-domain = { workspace = true }
